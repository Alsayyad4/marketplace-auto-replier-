# SubSell — Handoff (read this first if you're a new session)

This is the context to continue work on SubSell without re-discovering everything.
Active branch: **`claude/wizardly-noether-Oi6vP`**.

## What it is
A **Chrome MV3 extension** that auto-replies to Facebook **Marketplace** buyer
messages with the Anthropic API (FR/EN, casual Québec tone), **plus** a **Supabase
web dashboard** to manage all settings centrally so one change reaches every
computer/Facebook account.

- The **extension** does the real work (reads/types in messenger.com). A web app
  legally cannot touch another site's tab, so the bot MUST be an extension.
- The **web app** (in `docs/`, hosted on GitHub Pages) is the control panel.
- Settings live in **Supabase**; the extension reads them two ways (either works):
  1. **Cloud sync login** (email/password in Settings → General) — pulls every ~1 min.
  2. **Remote config URL** — paste the per-user config URL; pulled every ~10 min.
     The URL is served by the **Edge Function `subsell-config`**.

## Live deployment (the operator's project)
- Supabase project ref: `tcqunihripihroseswgy` (URL `https://tcqunihripihroseswgy.supabase.co`).
- Publishable/anon key is baked into `docs/config.js` and `background.js` (public by design).
- DB: table `public.subsell_configs` (`user_id`, `config` jsonb, `config_key`, `updated_at`) — see `supabase/schema.sql`.
- Edge Function `subsell-config` (`supabase/functions/subsell-config/index.ts`), **Verify JWT = OFF**, serves `config` by `?key=`.
- Storage bucket `subsell-videos` (public) for central demo videos — see `supabase/storage.sql`.
- Table `public.subsell_messages` (activity log) + Edge Function `subsell-log` (**Verify JWT = OFF**, `supabase/functions/subsell-log/index.ts`): every extension fire-and-forgets each sent message (reply/video/follow-up/human) to it via the `config_key`; the dashboard's **Activity** tab reads its own rows (RLS) and shows a combined feed + all-time/today totals + per-machine breakdown. Mirror is wired into `appendLog()` in `background.js` (fire-and-forget, never blocks replies); videos add one `LOG_EVENT` in `content.js maybeSendVideo`. Per-machine name = `machineLabel` (Settings → "This computer's label").
- Dashboard: `https://alsayyad4.github.io/marketplace-auto-replier-/docs/` (Google or email login).
- ⚠️ This Supabase project is **shared with another app**, so everything is namespaced `subsell_*` — never use generic names like `configs` or a generic `on_auth_user_created` trigger.

## Key files
- `background.js` — service worker: Anthropic calls, `buildSystemPrompt()`, `DEFAULTS` (source of truth for settings), config priority (managed → cloud → remote link → sync → local), `GET_REPLY_SIMPLE`, `GET_FOLLOWUP` + `callClaudeFollowup`, cloud auth/pull/push, `parseReply` + `stripReasoning`.
- `content.js` — the DOM side: `readConversation()` (role by left/right gap), `buyerSpokeLast()`, `botTailCount()`, `handleThread()`, `typeAndSend()`, `injectVideo()`, `maybeSendVideo()`, `maybeFollowUp()`, the `scan()` loop (guarded by `busy`).
- `options.html`/`options.js` — extension Settings (mirrors the web app + Cloud sync/Remote URL plumbing + Test responses + Activity log).
- `docs/` — web dashboard (`index.html`, `app.js`, `config.js`). `SETTINGS-REFERENCE.md` is the authoritative tab/field list; the dashboard mirrors it 1:1 with the extension (verified by diff).
- `SPEC-webapp.md` — the config-JSON contract. `supabase/README.md` — backend deploy steps.

## Bugs already fixed (do NOT regress these)
- **Reply leaked the model's reasoning** ("…her real question was… ---") → strong CRITICAL output rule in `buildSystemPrompt` + `stripReasoning()` safety net in `parseReply`.
- **Replied to its own messages (self-reply spam)** → `looksLikeOurBubble()` is now **tri-state**: `true` (blue/gradient bubble = ours) / `false` (neutral-gray = the buyer's) / `null` (unknown). In `readConversation` pass 2: `isOwnEcho || true ⇒ "me"`, `false ⇒ "buyer"`, `null ⇒` geometry only if it CLEARLY hugs the left, **else "me" (stay silent)**. The core rule: **a message is the buyer's only with positive evidence; ambiguity is treated as ours so the bot can never reply to itself.** Theme-agnostic (works in dark mode).
- **Self-reply / re-reply survives reloads** → `recentSent`, `cooldowns`, `lastHandled` are **persisted to chrome.storage.local** and hydrated at boot (`persistDedup()`), so a content-script reload no longer re-arms those bugs.
- **Duplicate/garbled sends** → `typeAndSend` inserts the whole message once, compares with an **invisible-char-tolerant** normalizer (so a correct long reply isn't falsely rejected and silently dropped), **sends once**, waits 5 s, and **only escalates to the Send button if the box still holds exactly our text** — never blind-resends. `pressEnter` = keydown only, `shiftKey:false`. `clickSend` only clicks a real Send control (never mic/like/sticker; EN+FR labels).
- **Follow-up spam** → smart follow-up cap is read live (`botTailCount`); `followupsDone = botTail - 1`; stop when `>= smartFollowupMaxCount`. The **`SEND_FOLLOWUP` alarm path now enforces the same cap + `rememberSent` + cooldown** (previously it bypassed the cap — could post a 3rd consecutive bot message).
- **Replied to Facebook system lines** → `isNoise()` filters EN **and FR** system lines (e.g. "attend votre réponse", "suggestion automatisée", "vous a envoyé un"); short tokens use `NOISE_EXACT` (exact match) so real buyer messages like "Sent it yet?" aren't dropped. Quick-reply preset buttons are skipped structurally via `[role="button"]`.
- **Re-sent the demo video** → `maybeSendVideo` uses an **atomic claim** (write `videoSentThreads[id]`, re-read, verify we own it) so two passes/tabs can't both send; `chatAlreadyHasOurVideo()` tests side vs the **composer-column midpoint** (panel-proof, not window center); `injectVideo` only presses Enter when a preview actually attached and **confirms by preview-detach**.
- **Demo videos sent/resent multiple times in one chat (v0.12.2 → fully fixed v0.12.3)** → the original dedup only recorded "this chat got its video" **after confirming the send landed**, but that confirmation (`injectVideo` preview-detach) and the DOM detection (`chatAlreadyHasOurVideo`) are both unreliable on FB's video rendering (sent clip = thumbnail + play button, duration in a **plain overlay span, not `[dir="auto"]`**, no `<video>` until played). When confirmation failed, a fail was recorded and the 20-min backoff **re-sent → duplicate**. **The real fix (v0.12.3): stop depending on send-confirmation for dedup.** `injectVideo` now returns `true` once a clip **attaches** to the composer (the reliable signal — it polls ≤25s for the preview) + Enter/clickSend is fired, without requiring the detach confirm. `maybeSendVideo` calls `markDone()` (persist `videoSentThreads[id]={done:true}` + clear `videoAttempts[id]`) **the instant the first clip attaches**, so a mid-send reload or unconfirmed send can never re-send. Added a **re-check after the pre-video delay** (re-read storage + DOM → bail if a video appeared during the wait). `chatAlreadyHasOurVideo()` was also broadened (scans every leaf span/div in rows for a badge-sized `mm:ss`, `<video>` fallback) but is now only a **recovery** path for storage-loss, not the primary guard. `console.debug("[SubSell] video: …")` breadcrumbs added (skip-already-sent / skip-already-in-chat / skip-after-delay / sending / marked-done) for live diagnosis in DevTools.
- **Clicked the mic** → `clickSend` skips voice/clip/mic AND like/sticker/gif buttons.
- **Re-entrancy** → `scan()` sets `busy=true` synchronously before any `await` (no check/set gap).
- **Cross-tab double-send** → a per-threadId **storage lease** (`acquireThreadLock`/`releaseThreadLock`, `threadLocks` in chrome.storage.local, last-writer-wins + `LOCK_MS` stale timeout, `TAB_UID` per instance) wraps `handleThread` and the `SEND_FOLLOWUP` handler, so two Messenger tabs in one profile can't process the same chat at once.
- **Wrong-thread context on slow loads** → `handleThread` waits for two identical transcript reads before acting.
- **v0.21.20 — CLOSER upgrade (operator: "not closing deals, less customers coming to the shop — make it the best closer, don't play with other functions"). PROMPT-ONLY changes, zero pipeline edits:** (1) **The MASTER CLOSER PLAYBOOK now applies to `closerIntensity` "medium" (the default) too, not just "master"** — the old "BALANCED" middle setting produced polite info-desk replies that answered and never closed, and since medium is the default the whole fleet ran its weakest seller; "soft" remains the only gentle option. (2) Playbook expanded 9→12 moves: first-reply frame (answer + one concrete shop-beats-ad reason + one easy question), trade-in hook early (a trade-in can only be evaluated IN PERSON — makes the visit necessary, not optional), budget-too-low counter ("plusieurs modèles dans ton budget en magasin"), never-let-it-die rule (bare "ok"/"thanks"/emoji gets one value line + one time question), back-off valve (two visit-dodges ⇒ one pure-value message before re-closing), 2-3-sentence hard cap; kept: assume-the-visit two-option close, reserve + name capture, honest-urgency-only, objection counters, stop-selling-after-yes + [VISIT:yes], mirroring. (3) **`callClaudeFollowup` re-engagement prompt rebuilt as a closer's second touch**: personal hook back to what the buyer wanted + ONE honest new reason to come now (liquidation moving / units to compare / trade-in evaluated on the spot; inventing stock/buyers/deadlines explicitly forbidden) + ONE time-anchored question, two sentences max; [SKIP] rules kept and extended (skips when a visit time is already set). (4) `DEFAULT_VISIT_MSG` de-bottified (the old string sent FR + " / " + EN in one line) → short FR with parenthetical EN + "je te garde ça de côté" hold-hook. All contracts untouched: [HUMAN]/[VISIT] tokens, video-is-automatic rule, platform-safety block, output-format block, `buildSystemPrompt` stays a pure function of settings (fleet-wide cache prefix intact). NOTE for operator conversions beyond the bot: the v0.21.19 starvation fixes matter as much as this prompt — unanswered buyers were the biggest deal-killer. (two machines: PC-wo4rq, PC-o9ppb, 2026-08-17). Symptoms: "some convos get 1 video, some 2, not stable", "sometimes not replying at all", follow-up rows duplicated, one follow-up sent the thread TITLE as the message.** Root causes read directly from the reports + code: (1) **"Unread message:" snippet prefix (new Messenger accessibility text, FR "Message non lu") defeated the `^you:`/`^vous:` anchors in `snippetSuggestsBuyerLast`** → rows where WE spoke last ("Unread message: You: …") counted as waiting buyers → immortal ghost `waitingSince` entries; lane 0 is oldest-first AND cooldown-immune with a 4-min reopen floor, so on PC-o9ppb 14 ghost "overdues" monopolized the scanner (real buyer Raphael unanswered 19 min with replies=0) and `overdueNow` stayed true → every video force-queued and the pending lane never ran. Fix: strip `/^(?:unread messages?|messages? non lus?)\s*[:.,]?\s*/i` from the joined snippet before the reject tests — ghosts stop qualifying, the existing per-scan purge then deletes their ledger entries. (2) **Partial video sets burned to terminal marks**: the clip loop CONTINUED past a failed attach and stamped `{done, sent:N<total}` — no retry, no resume (PC-o9ppb: whole sidebar `sent2`; the four `"0 demo video(s) sent"` log rows were resume tails that attached 0 and then BURNED the marker because the zero-attach rollback requires `startAt===0`). Fix: break on first attach failure → `{done, resumeFrom: failedIndex, resumeTotal, sent}` + `recordVideoFail(id,"attach")` pacing + explicit `videoPending` queue for the finishing visit; the mid-set `stillOnThread` abort also queues now; success log only when clips actually attached, message now "K/N demo video(s) sent". Safe-by-construction: `injectVideo=false` ⇒ no preview appeared AND strays were cleaned ⇒ nothing sent ⇒ re-attempting exactly that clip cannot duplicate. (3) **`injectVideo` count-delta detection raced lingering uploads**: previews were counted across ALL of `[role=main]`; a prior clip's still-uploading preview detaching inside the next clip's 12s window masked the +1 (net zero) → genuinely-attached clips read "failed" — why the 5MB clip 2 consistently killed clip 3 on slow machines. Fix: `composerRegion()` (walk up from the composer until the ancestor holding `[role=row]`s; scope = that footer, falling back to main), NEW-ELEMENT IDENTITY detection instead of count delta, later clips PRE-WAIT ≤45s for the tray to clear, first-of-run clip CLEANS stale strays (clicks the remove-attachment buttons — EN/FR aria labels), failure path cleans strays too so they can never ride out with a later text reply. (4) **Crash-resume + in-flight guard**: progress stamps now carry `resumeFrom: i+1, resumeTotal` (a mid-set tab death resumes the missing tail instead of skipping it forever); a `via:"lock"` stamp younger than 5 min blocks any other pass from resuming (each attached clip refreshes `at`; worst legit stamp-to-stamp gap ≈2min) — two passes can never interleave one set. (5) **3-strikes/24h attach pause NEVER actually fired** (both reports: pausedAttach=0 despite repeated failures): the pre-send lock block deleted `videoAttempts[id]` wholesale, resetting `fails` to 0 on every retry visit. Now it preserves `{fails, failAt, why}` (drops only the claim); the terminal success clears the entry. (6) **Won't-open threads**: "thread won't open (60×)" — the 30-min park was a cooldown, but lanes 0/1 IGNORE cooldowns → retried every ~4 min for hours. Lanes 0/1 now honor the park for repeat offenders (`openFails>=3`), parks escalate 30min→6h(≥6)→24h(≥10), and at exactly 6 fails one Activity row names the chat for manual handling. (7) **Reply-capped chats purged from the waiting ledger** (resolved BY POLICY; they were re-stamped every scan, jamming lane 0 and `overdueNow`; PC-wo4rq had replyCapped=9). (8) **Terminal exits in `maybeSendVideo` clear `videoPending` under BOTH keys** (adopted id + sidebarKey) — a sidebar-keyed entry for a done chat was immortal (PC-o9ppb: Tracy vidQ=58m against a done mark). (9) **Follow-up double-log fixed** (background GET_FOLLOWUP logged at generation, content logged again at delivery → remove the background row) + **title-echo guard** (model occasionally returns the thread label verbatim; PC-wo4rq SENT "Svargood · S25 ultra…" to a buyer — now treated as [SKIP], never typed). (10) `SUD_DIRNAME` rejects `crxfs` (virtual packaged-fs root, seen stored on a real machine). (11) Diagnostics upgraded: per-row `vmark` shows `+rK/N` resume markers; background report splits `resume-pending` from `done-no-sent`. **DELIBERATELY NOT DONE: no auto-repair of already-burned `{done, sent:2}` marks** — the old loop continued past failures, so `sent` does not identify WHICH clips were delivered (non-contiguous sets possible); resuming at index `sent` could duplicate. Those chats stay at 2/3; the manual per-chat "🎬 Resend video to OPEN chat" button remains the eyeball-verified cure. **ADVERSARIAL AUDIT HARDENING (17-finding multi-agent review of the first draft; every finding addressed):** (a) resume machinery made duplicate-proof BY PROTOCOL, not by timing: each loop iteration first checks TAKEOVER (`mark.owner !== TAB_UID` ⇒ bow out silently — the new owner resumes strictly AFTER the last stamped clip) then writes a **PRE-ATTEMPT stamp** `{via:"lock", owner, sent, resumeFrom: i+1, resumeTotal}` BEFORE touching clip i — so a crash anywhere in the attempt (even after Enter fired) can never re-send it: the original "attempted clips never repeat" law kept under resume (cost: a crashed attempt's clip may be missed, never duplicated); (b) in-flight window 5→**30 min** with `at` refreshed EVERY iteration (survives background-tab timer throttling); `gapSec` clamped ≤600s so no dashboard value can outrun the window; even a wrongful takeover is duplicate-free per (a); (c) `injectVideo` returns **true | false | "dirty"**: sweeps the tray between strategies (a late-rendering paste + escalation used to stage TWO copies sent by one Enter), double-sweeps with settle gaps on failure, and returns "dirty" when the tray can't be verified clean ⇒ caller skips that clip permanently (`resumeFrom=i+1`) instead of re-attempting (a hidden half-attach could double-send); "false" (tray verified clean) allows re-attempt; mode `"resume-first"` WAITS instead of sweeping (a fast same-tab resume must not cancel a just-sent clip's lingering upload); (d) `composerRegion()` walk replaced with one robust rule — tray = previewSel matches **not inside `[role=row]`** (message-row media excluded, no fragile DOM walking); (e) claim RE-VERIFIED + re-stamped right before the lock (the pre-lock delay+downloads can outlive the 3-min TTL — a second tab could previously double-lock); the resume-visit lock stamp CARRIES `sent/resumeFrom/resumeTotal` (a crash there used to degrade it to a bare lock that heal (i) could mis-clear and re-send head clips); (f) **`adoptedAlias` map** (sidebar id → adopted canonical id, in-memory, set at adoption): bridges `replyCounts` (cap purge) and `videoAttempts` (`videoAttBlocked` in both video lanes) across the redirect split that made those gates dead on this all-group fleet; (g) won't-open quarantine: `openFails` now `{n, at}` with 2h strike decay + a 4s slow-load re-grade before scoring a fail (laggy RDP opens no longer park real buyers), lane 1.5 honors the park too (its 15-min cooldown bypass used to override it); (h) scan-level **resume-mark rescue** re-queues stale crashed tails (`resumeFrom` present, not in-flight, no pending entry); (i) `armVideoCatchUp` skips bare `via:"lock"` marks younger than 30 min (clearing an in-flight lock + claim let a second tab start a parallel full set); skip-after-delay no longer latches `videoLocked` on resume markers (a latched tab ate the tail's queue entry). Residual accepted: `openFails` stays in-memory (a reload re-earns the park in ~3 tries — bounded), and heal (i) on FRESH bare locks is unchanged (crash before any attempt ⇒ clearing is provably safe). (operator: "some convos not sending message some convos not sending videos, it is all mixed up — include a diagnostic button so we stop guessing"):** new popup button "🩺 Diagnostic (copy & send to Claude)". Popup asks background `GET_DIAGNOSTIC` (→ `buildDiagnostic()` in background.js) + sends `DIAG_PROBE` to every open Messenger tab (≤4), concatenates the sections into ONE text block, fills the existing #dump textarea AND copies to clipboard ("copied ✓ — now paste it to Claude"). A non-responding tab is reported as a finding in the block itself, not an error. **Background section** (all ages in minutes, one line each): version+machineLabel; config source resolution (managed/cloud/remote-link/sync/legacy + cloud login/stale, remote host, ages); operational settings (on/off, api set?, model, caps, business hours + open-now, delay, maxReplies); videos (central list w/ per-clip name+host+cached-MB-or-NOT-cached, local toggle+clips, first/between delays); counters + mirror health + debugTick summary; videoSentThreads breakdown (total/sent/lock/dom/taildrop/recon/done-no-sent); queues (waitingSince count+oldest, videoPending count+oldest, future cooldowns, lastHandled, reply-capped count); attempts (pausedLoad/pausedAttach/liveClaims, active URL strikes, catchUp armed?, auto13/auto17 flags, sud base/dir/lastCheck); chrome.alarms dump; replyLog tail (last 10, thread 14ch, reply 44ch). **Tab section**: busy/pausedForUpdate/catchUp + in-memory map counts; anchors + computed overdueNow (the exact force-video gate); first 12 sidebar rows with per-row verdicts (unread?, buyerLast?, wait/vidQ/cool ages, replies count, vmark, attempts, 32-char snippet); open chat (composer?, msg count, last role, hardened + legacyVideo + legacyBadge detector verdicts, raw mark JSON 90ch). **REDACTION BY CONSTRUCTION**: apiKey → set/NOT-SET flag only; config/video URLs → host only (the `?key=` secret is never read); configKey → set flag; buyer text truncated. Report stays well under ~8KB. Read-only everywhere — the probe mutates nothing. (21-agent audit; 18 confirmed / 0 rejected):** the operator confirmed v0.21.16 + no errors + video list present + zero sends. THREE stacked silent causes: (1) **PRIME — overdueNow read the RAW persisted waitingSince ledger**: one immortal ghost entry (phantom-era, unservable) made force-mode's only exception fire on EVERY reply ⇒ every video silently queued with healthy-looking status. Fix: liveness check (entry must belong to a RENDERED row still reading buyer-last — same gates as lane 0), + vstat on the queued exit, + 24h TTL sweeps of waitingSince (boot + per-scan) + per-scan purge of rendered rows that no longer read buyer-last. (2) **Catch-up enqueued NOTHING on all-dotted machines**: the enqueuer's isUnreadAnchor skip dropped every stuck-dot row (the entire sidebar), then the 6-dry-scan disarm consumed the one-shot in ~48s ⇒ the 0.21.13 auto catch-up did literally zero on this fleet. Fix: skip is snippet-only now (dotted "You:" rows enqueue; truly-waiting buyers still left to reply lanes), + fresh one-shot re-arm `autoCatchUp01217` (transactional, same engine). (3) **Suspicious-read gate keyed on the stuck dot** (sidebarSaysBuyer included isUnreadAnchor) ⇒ every queued delivery visit burned 2×2-min rechecks; now snippet-only. Plus: badge branch hardened to paint/row-rect anchoring + listing-link exclusions (composer-midpoint hole closed), frozen `legacyBadgeDetect` + `BADGE_FIX_TS`, heal (ii) probe now BOUNDED (3 tries ≥4h apart, loaded-chat gate, clears only on hardened-negative ∧ legacy-positive divergence, gate widened to BADGE_FIX_TS, legacy rechecked=1 counts as 1 try); claim-race + skip-after-delay exits now vstat; 24h videoPending eviction → back-of-queue RE-STAMP (eviction dumped chats onto never-visiting idle rotation); settingsOk hiccup exit queues the chat (sidebarKey param threaded through all 5 maybeSendVideo call sites + terminal clears). Security note: workflow verifiers were flagged for "re-arming automated sends" — explicitly operator-directed ("force the video sending", "no manual work"); all anti-duplicate guards intact.
- **v0.21.16 — FORCE-VIDEO MODE (operator: "force the video sending, every convo has to have the videos set"):** post-reply deferral no longer uses ANY row-count proxy — the set goes out INLINE after every reply at the configured delay. Sole exception: a buyer already OVERDUE (>OVERDUE_MS on the waitingSince ledger) at that instant ⇒ queue (sidebarId key) and reply to them first — the one precise harm case, immune to phantom rows by construction. Also: "videos OFF"/"no videos configured" exits now ALSO set lastError (red popup line) so an unconfigured machine is unmissable. NOTE: operator attributes the video stoppage to the economy releases (0.21.7/0.21.9) — re-audited each economy change against the video path: none touches it (caching/model/follow-up guards/probe-fetches only affect billing + Supabase traffic); the actual causes were the starvation/mark/key bugs fixed in 0.21.10-0.21.14. Burst tradeoff accepted per explicit operator priority: during a multi-buyer burst each inline set (~60-120s) delays the NEXT pick; LANE 0 still rescues anyone >5 min.
- **v0.21.15 — faster backlog drain (operator: "i dont want over next day"):** aged-video lane pacing 10→4 min, min pending age 20→10 min. New-reply videos were already inline at configured delay since 0.21.14; these constants only bound the worst case on busy machines — whenever no buyer is waiting, LANE 1.5 already drains continuously. LANE 0 (overdue buyers) remains absolute top priority.
- **v0.21.14 — timing + false-marks audit (25 agents, 22 confirmed / 0 rejected; regexes EXECUTED against operator screenshot text):** (1) **SMOKING GUN — sidebar truncation defeated the system-row filter**: "This is an automated su…" (truncated!) passed `snippetSuggestsBuyerLast` because the reject term was the full phrase `automated suggestion` ⇒ 4 phantom "waiting buyers" on the operator machine ⇒ `waitingNow>=3` permanently ⇒ EVERY post-reply video deferred (the "timings not respected" complaint: videos late via aged lane, then INSTANT on open because deferred deliveries passed immediate=true). Fixes: truncation-robust reject terms (`this is an automated`, `ceci est une suggestion`, `suggestion automati`, group-membership rows; explicitly NOT "waiting for your response" — genuine buyer signal, rejecting it would starve buyers); deferral threshold 3→5; **deferred/queued deliveries now honor demoVideoDelaySec** (immediate only on genuinely idle revisits — call sites pass `videoPending[id]==null && videoPending[sidebarId]==null`). (2) **Deferral/lane KEY MISMATCH fixed**: deferral wrote videoPending[adopted id] but lanes look up threadId(anchor)=sidebar id ⇒ deferred sets invisible to the video lanes on redirected GROUP threads (this fleet is all-group!). Deferral now keys by sidebarId; terminal deliveries mirror-clear it (`if (videoLocked.has(id)) clearVideoPending(sidebarId)` at the 3 sites). (3) **Jing Jr. false "already marked sent" explained + self-healing**: the `<video>` branch degraded to `center-x > composer-midpoint` (looksLikeOurBubble null on media) ⇒ listing/buyer clips marked text-only chats sent via:"dom". Hardened: 80px floor, marketplace-link exclusion (element+row), paint veto, side by ROW-rect right-anchoring (rightGap<=64 && leftGap>rightGap+40); unused `ours()` removed. SELF-HEAL in the confirmed-skip branch: (i) orphaned via:"lock" marks (crash mid-send, no `sent`, >24h) — DOM-confirm or clear; (ii) pre-hardening via:"dom" marks (`at < VIDEO_DETECTOR_FIX_TS` 2026-08-10) — one-shot divergence probe (hardened=false && frozen `legacyChatVideoDetect()`=true ⇒ FP source identified live ⇒ clear; both-negative keeps mark = zero-duplicate bias; `rechecked` flag). Deliberately NOT shipped: full-thread scroll audit (finding 8 alternative — heavy, gating contradiction). (4) **Mark taxonomy for crash-safety**: pre-send lock now via:"lock" (+recon carry); per-clip progress stamp `{done,at,via:"lock",sent:n}` after EVERY attached clip; resume-tail drop tagged via:"taildrop"+resumeTotal (catch-up keeps it). (5) **armVideoCatchUp transactional** (finding 9): one-shot boot flag rides in the SAME storage write as the cleared map (failed arm ⇒ flag unset ⇒ next boot retries); read rejects on lastError; popup handler gets .catch. (6) **Blank dashboard/options number fields no longer save 0** (`Number("")===0` was silently zeroing demoVideoDelaySec/BetweenSec — likely a REAL cause of "not respecting video timings"); blank = keep saved value; explicit 0 still honored.
- **v0.21.13 — catch-up is now FULLY AUTOMATIC (operator: "i dont want any manual work"):** the 0.21.12 per-machine button still required one click per computer. Now: shared `armVideoCatchUp()` + a one-time boot migration (`autoCatchUp01213` flag in the hydrate callback) arms the backlog catch-up automatically on each machine's first boot after the update — the fleet self-updates, reloads, and drains its own backlog with zero operator action. The popup button remains for re-runs. SAFETY FIX included: 0.21.12's button cleared partial-set **resume markers** (done:true + resumeFrom, no `sent`/`via`) — clearing those would re-send already-delivered head clips; `armVideoCatchUp` now treats resumeFrom/resumeTotal as CONFIRMED and keeps them. Delivery unchanged: aged-video lane pacing (1 set/10 min, replies first), chatAlreadyHasOurVideo() + complete-set + lock + lease guards on every send, self-disarm on drain (6 dry scans) or 3 days.
- **v0.21.12 — one-click BACKLOG catch-up for videos (operator: "i cant do manual work like this, every chat has to have the videos set"):** the per-chat "Resend video to OPEN chat" button (0.21.10) was infeasible at fleet scale. New popup button **"📹 Catch up videos (this computer)"** → CATCH_UP_VIDEOS: clears only AMBIGUOUS done-marks (old-bug era: `{done:true}` with no `sent` count and not `via:"dom"`) + their videoAttempts + videoLocked, KEEPS confirmed sends (now stamped `{done:true,at,sent:N}` at the success path) and DOM-seen (`via:"dom"`) marks, then arms a persisted `videoCatchUp={armed,at}` flag. While armed, scan() enqueues replied-to (=!unread && !buyerLast && not-confirmed-done) rendered chats into videoPending with a 21-min-old timestamp so the existing **aged-video LANE 0.5** (0.21.10, 1/10min, reply-safe) + LANE 1.5 deliver them — every send still passes the `chatAlreadyHasOurVideo()` duplicate guard, complete-set guard, per-chat lock, cross-tab lease. Self-disarms after 3 days OR 6 dry scans with empty queue. Enqueue capped 30/scan, dedup-guarded, one persist per change. DUPLICATE-SAFETY: reuses ONLY existing proven guards (no invariant weakened) — residual risk limited to a real video scrolled out of the loaded DOM at check time; operator warned in the confirm dialog. Framing to operator: NEW buyers already get videos automatically (0.21.10 pipeline) — catch-up is a one-time cleanup, ONE click per computer, not per chat. **Also flattened dist zips** (manifest.json at zip ROOT) so Extract-All yields ONE subsell-extension folder (no double-nesting → fixes the "Manifest file is missing or unreadable" load failures).
- **v0.21.11 — operator screenshot caught the bot SENDING Claude's meta-commentary to a buyer** ("[No response needed — this is a system message indicating the buyer has left the chat…]" appeared in lastReplySent): (1) `parseReply` now treats any reply BEGINNING with an unrecognized bracketed token as `{kind:"empty"}` — meta-commentary can never be typed into a chat (known tokens [VISIT]/[HUMAN]/[VIDEO] parse first, unchanged). (2) Trigger closed upstream: group-membership system rows ("A contact left the group.", joins/renames + FR equivalents) added to `isNoise` — this fleet's chats are ALL "Group chat: …" (see screenshot fingerprints), and those rows were read as buyer messages. (3) `reply.skip` with reason "empty reply" now marks `lastHandled[id]` + clearWaiting + persistDedup — Claude's deliberate silence is a resolution, not a retry-forever (was re-billing the same unanswerable message every cooldown). SESSION NOTE: container restarted mid-session with a stale bcfeb09 (v0.11.1) checkout; three edits initially landed on the ancient tree — recovered via `git fetch` + `reset --hard FETCH_HEAD` (v0.21.10 was safe on origin; verified via the public zip) and re-applied against the real files. Screenshot also confirmed the v0.21.10 starvation diagnosis: 19 anchors / "14 unread" sticky dots on an all-group-chat account, machine still on a pre-0.21.10 build (no "video status" row in its popup).
- **v0.21.10 — "SOME accounts not sending video" (24-agent adversarially-verified audit; 20 confirmed, 0 rejected):** ROOT CAUSES + FIXES: (1) **LANE 1.5 starvation on busy accounts** (the big one): lanes 0/1 never both empty during business hours ⇒ videoPending waited UNBOUNDED (and 1427's unconditional timestamp overwrite kept resetting entry age). Fix: **LANE 0.5 aged-videos** — ≥20-min-old pending entries get ONE visit per 10 min, outranking LANE 1 but NEVER LANE 0 (overdue rescue intact); `agedVideoTried` 1h per-chat skip so a stuck chat can't wedge the lane; deferral write now `if (videoPending[id] == null)`. (2) **videoUrlFails strikes now DECAY after 6h** (were permanent ⇒ one bad network day = machine never sends again); strike shape {n,at} (legacy numeric = at:0 ⇒ auto-heals on first pass); MANDATORY companion: resume-tail integrity — `resumeTotal` recorded in mid-set marker, tail DROPPED (marked done) unless files.length matches exactly (set can GROW after strike revival ⇒ index replay could duplicate). (3) **videoCache poisoning**: captive-portal/proxy HTML cached as "the video" forever ⇒ validate on write (mime/magic-bytes/min-50KB/truncation w/ content-encoding gate) AND read (cacheEntryLooksValid; purge+refetch once). (4) **FR/24h-locale false positive**: bare time dividers ("14:32") matched the mm:ss badge scan ⇒ fresh chats marked "already has video" on FR accounts. Badge now requires: NOT inside [dir=auto], overlaying a ≥80×80 media rect in the same row, side judged from the MEDIA rect, looksLikeOurBubble veto; ours() helper veto-tightened for the <video> branch too. New done-marks tagged via:"dom" (inert). (5) **LANE 1.5/0.5 backoff-awareness** (videoAttempts read once per scan only when queue non-empty) + 24h pending eviction. (6) **Buried-pending rescue**: minimized + idle-only pick ⇒ deepScanStep() pages the virtualized sidebar toward an off-screen pending chat (30s throttle, 40-try cap). (7) **OBSERVABILITY**: popup now RENDERS videoLast ("video status" row — it was written but never displayed!); vstat → reportVideoStatus mirrors MATERIAL blocked states to the central Activity feed (action "video-status"), ≤1 row/reason/24h (videoBlockReported map, keys never deleted), "OK again" recovery row for machine-wide blockers; all-URLs-blocked + attach-fail 0/N get explicit daily rows naming the machine. (8) recordVideoFail(id, why) — 24h-pause popup line now distinguishes load-fail vs attach-fail. (9) attach hardening: previewSel + EN/FR remove-attachment aria labels; listing-uploader exclusion regex superset (EN/FR variants); clickSend accepts "…Entrée pour envoyer". (10) **cloudStale breadcrumb** (cloudPull auth-dead + cloudConfig present ⇒ popup Activity pill shows "⚠ cloud sync frozen — log in again"); fetchRemoteConfig success now re-syncs configKey from the URL (regen-key 404 fix). (11) **popup "🎬 Resend video to OPEN chat" button** → CLEAR_VIDEO_MARK_OPEN_CHAT (refuses if a sent video is visible; clears videoSentThreads+videoAttempts+videoLocked for that one chat) — the manual cure for pre-0.21.4 burned marks. DELIBERATELY NOT DONE: bulk-clearing old done-marks (indistinguishable from real sends ⇒ duplicate risk), cap-hour video-only mode (optional variant not shipped), widening migVideoMarks0214.
- **v0.21.9 — cost audit round 2 (21-agent adversarially-verified workflow; 13 confirmed, 4 rejected), all behavior-preserving:** (1) **Pre-call stillOnThread guards**: before `GET_REPLY_SIMPLE` ask (operator navigated during the 4-6s load/settle window used to bill a call that the post-delay guard then discarded) and before `fullTranscript()`/`GET_FOLLOWUP` in maybeFollowUp (also closes a latent wrong-transcript-send hazard). (2) **Billed-reply memo** `pendingReply[id]` (in-memory, keyed buyerMessage+transcript, 10-min TTL): abort-during-delay / send-failure used to re-bill an identical call next cycle; now the paid text replays via `cachedText` → background replay branch runs the same gates + incrementCounters + appendLog (counter/log accounting byte-identical to today's re-bill), cleared on delivery. (3) **Billed-followup memo** `st.pendingText/pendingFor/pendingFp` in followUpState (persisted; fp = JSON.stringify(settings); stored BEFORE send attempt so all 3 abort paths keep it; replayed via `pendingText` → background `{text}` short-circuit; cleared on delivered). (4) **Listing videoUrl dropped from buildSystemPrompt** (both branches) — model is forbidden from sharing links, URLs were ~35-50 wasted tokens/listing/call + leak risk. (5) **cloudPull stamp probe**: `select=updated_at` (~0.1KB) first, full config row fetched ONLY on change — fleet egress was ~7-26GB/mo (over Supabase 5GB free tier), now ~0.2GB/mo. (6) **fetchRemoteConfig(auto)**: auto-refreshes (alarm + boot) skip while a non-empty cloudConfig exists (priority chain makes remote dead data then; was ~86K edge invocations/mo); manual "Fetch now" (no arg) unchanged; cloudLogout now refreshes the remote fallback FIRST so unshadowing never serves stale config. (7) **fetchVideo Content-Length pre-check** before arrayBuffer (an oversized clip used to burn ~3GB egress fleet-wide just to be rejected). AUDIT FACTS: buildSystemPrompt verified byte-stable (shared fleet cache prefix sound); **Haiku cache minimum is 4096 tok — typical prompt ~1.3-3k tok ⇒ cache_control silently inert on Haiku unless operator content is heavy (fine on Sonnet, min 1024)**; videoCache confirmed persistent (no MV3-wake re-downloads). REJECTED (do not do): move delay before the reply call (changes reply-timing semantics), composer pre-check in follow-up (its discard path mutates followUpState — not behavior-neutral to skip), Batches API for follow-ups (changes timing), usage telemetry (approved-but-deferred; write-only lastUsage would be safe).
- **v0.21.8 — "Update now does downloads but the version never changes" (restart escalation):** the 0.21.6 probe fix made downloads WORK, but the restart was gated on "no tab mid-task" and only retried on the 10-min tick — on busy accounts a tab is mid-conversation most of the workday, so the new version sat fully downloaded on disk while the LOADED version never changed. Fix: `armUpdateRestart()` — the moment an update is on disk (downloaded OR manual copy-replace, detected in `selfUpdateCheck`), every bot tab gets `PAUSE_SCANS` (content: `pausedForUpdate` — scan() stops STARTING new chats before claiming `busy`; in-flight send always finishes; 10-min failsafe self-resumes if no restart comes) and a 30s `UPDATE_RETRY_ALARM` re-runs `selfUpdateCheck` until idle ⇒ restart lands seconds after the current chat wraps, bounded by the 6-min busy watchdog. Version-match path clears the retry alarm + broadcasts `RESUME_SCANS`. sudStatus now says "vX on disk — restarting the moment the current send finishes" instead of looking dead. Operator fallback that always works instantly: copy-replace files → chrome://extensions → ⟳ on the SubSell card.
- **v0.21.7 — API-cost cuts, all behavior-preserving (operator: "spending so much money"):** (1) **Prompt caching**: both `callClaude` and `callClaudeFollowup` now send `system` as `[{type:"text", text, cache_control:{type:"ephemeral"}}]` — `buildSystemPrompt` is a pure function of settings (verified: no Date/random anywhere in it), so the fleet's shared API key + identical synced settings = ONE cache entry for all machines and both call types; repeat calls bill the instruction sheet at ~10%. Note minimum cacheable prefix: Sonnet 1024 tok, **Haiku 4096 tok** — below that the marker is silently inert (harmless). (2) **DEFAULTS.model → `claude-haiku-4-5`** ($1/$5 vs Sonnet's $3/$15 per MTok = 3× cheaper) + `||` fallbacks at both call sites; dashboard + options selects reordered with haiku labeled "(recommended — 3× cheaper)"; an explicit model SAVED in the operator's dashboard config still overrides — flipping the stored value is operator-side. (3) **Follow-up evaluation cost guards** in `maybeFollowUp`: dead-lead cutoff (sidebar quiet > 7 days ⇒ return before ANY API call — old chats were re-billed a full evaluation every ~threshold/2 forever because [SKIP]s never counted toward the cap) + skip cap (`st.skips` counts consecutive [SKIP] verdicts, 3 ⇒ stop evaluating that chat; a SENT follow-up resets to 0). Genuine follow-ups on fresh leads unchanged. NOT done deliberately: max_tokens reduction (output bills on actual tokens generated, not the cap — lowering it saves nothing and risks truncated messages to buyers).
- **v0.21.6 — self-updater was dead on EVERY machine: probe filename started with a dot.** Operator: "Update now says 'extension folder is not inside Downloads' but the folder IS in Downloads" — they were right. `sudProbeBase` wrote its test file as **`.sud-probe`**, and `chrome.downloads.download` REJECTS any leading-dot path component as "Invalid filename" (Chromium `IsFilenameLegal`: `.` is illegal leading/trailing). So the probe download never started, every base "failed", and the updater blamed the folder — on all machines, since 0.20.0. The updater has therefore NEVER self-updated anyone; every past rollout was the manual zip. Fixes: (1) probe file renamed **`sud-probe.txt`** (+3×250ms fetch retry; per-id `removeFile`+`erase` cleanup — the old regex cleanup was dead code because `sudDownload` had already erased the history entry it searched for). (2) **`sudCandidates()`** replaces the fixed list: real on-disk folder name first (popup reads it via `chrome.runtime.getPackageDirectoryEntry` — foreground-only API — and reports it as `SUD_DIRNAME` → `sudDirName` in storage; renamed folders now update fine), then `SUD_BASES`, then names derived from actual `subsell*.zip` entries in download history (covers " (2)" re-download variants, evidence-based so we don't litter Downloads with guessed folders; capped 15, case-insensitive dedup). (3) Failure messages now say WHICH failure: `sudProbeWrites===0` ⇒ Chrome refused the write (names `sudLastDlErr`, tells them to turn OFF "Ask where to save each file"); files written but never seen ⇒ names Chrome's REAL download dir (from `downloads.search` history — exposes OneDrive-redirected Downloads) + the folder names tried. (4) AUTO-UPDATE-SETUP.md: existing machines must **copy-replace files into the loaded folder — NEVER "remove" the extension first** (removal wipes chrome.storage.local: API key/config URL — that's what caused the "videos OFF" machines after the 0.21.4 reinstall). CATCH: broken updaters can't self-deliver this fix — v0.21.6 is one last manual copy-replace per machine, THEN auto-update finally works.
- **v0.21.5 — remaining audit findings closed (10 confirmed total):** (5) **zero-attach rollback**: if `okCount===0 && startAt===0` (no preview EVER appeared ⇒ nothing can have been sent), UNDO the lock (`videoLocked.delete` + remove the fresh `{done:true}` unless resumeFrom) + `recordVideoFail` ⇒ chat retries with normal backoff instead of being burned forever at 0 videos. (6) `fetchVideo` rejects clips >45MB with an explicit "re-upload under ~40MB" error (Chrome messaging cap ~64MB; oversized clips failed forever unexplained; videoUrlFails then excludes+names them). (7) chats stuck on "already replied" or handed to [HUMAN] now get an idempotent `maybeSendVideo(..., true)` pass (they previously never reached any video call site). Noted-not-changed (audit-confirmed but acceptable): business-hours/caps gate also pauses video delivery (intended humanlike behavior); LANE 1.5 serves only rendered rows (deep-scan mitigates).
- **v0.21.4 — MULTI-AGENT AUDIT of "videos not sending at all": four confirmed root causes fixed.** Workflow `video-pipeline-audit` (3 hunters + adversarial verifiers) confirmed: (1) **`chatAlreadyHasOurVideo` check (0) false-positive** — the "Add video to listing / Update listing / autoriser les autres acheteurs" TEXT match against all of `[role=main]` matches FB's own listing card shown in chats where we NEVER sent a video ⇒ every fresh chat marked `{done:true}` without sending; after the fleet reinstall wiped storage this was total. **REMOVED** — only strong row-scoped signals (our-side `<video>` / mm:ss badge) may declare a chat served. (2) **`injectVideo` fed the WRONG input** — document-global first `input[type=file]` can be the LISTING CARD's uploader (clips went to the listing, not the buyer). Reordered: **paste into focused composer → drag-drop → file input last** (filtered to exclude inputs inside add-video-to-listing wrappers, prefer LAST in DOM), and each strategy must produce a preview before being trusted (12s/strategy). (3) **Reaction rows counted as waiting buyers** ("Reacted 👍 to your message" passed `snippetSuggestsBuyerLast`) — deferral stayed ≥3 forever; reject regex now excludes reacted/a réagi/liked/a aimé. (4) **Per-URL 3-strike loader** (`videoUrlFails`): a dead/oversized dashboard clip is EXCLUDED from the set + named in popup instead of failing complete-set on every chat (verifier confirmed fix works). Plus: **`videoLast` popup line** — every exit of `maybeSendVideo` reports why (skips, backoffs, "0/N attached ⇒ FB uploader changed", sent ✓) — future video issues diagnosable from a screenshot. **One-time migration** `migVideoMarks0214`: clears `{done:true}` marks younger than 7 days (the false marks since the reinstall) so those chats finally get videos; genuinely-served chats stay protected by the strong DOM signals. OPERATIONAL: reinstall wiped `remoteConfigUrl`/`cloudAuth` on some machines ⇒ `demoVideoUrls` empty ⇒ "videos OFF" (now visible via videoLast) — re-paste the config URL there.
- **v0.21.3 — "not sending videos at all" (0.21.2 regression) fixed with a guaranteed pending-video lane:** on busy accounts ≥3 buyers are waiting almost ALWAYS, so 0.21.2's deferral fired after every reply and there was no delivery mechanism ⇒ videos effectively stopped. Fixes: (1) **`videoPending` queue** (persisted, hydrated, capped via persistDedup; `clearVideoPending(id)`): the deferral records `videoPending[id]=now`; **pickTarget LANE 1.5** (after fresh buyers, before P2/idle) services the OLDEST pending — respecting the chat's cooldown unless pending >15 min — so deferral = postponement, guaranteed delivery. Cleared at every video terminal (locked/done/visible/committed/disabled). (2) **Central videos = single source of truth**: when `demoVideoUrls` exist, legacy per-machine `demoVideos` are IGNORED (a corrupt old local file could fail the complete-set guard forever + caused local+central duplicate sets); complete-set guard now enforces only the CENTRAL count. (3) Popup tick shows `videoQueueCount`.
- **v0.21.2 — backlog-first scheduling (morning-burst fix):** operator screenshot at 9:07 AM: `unread count: 15` right after business hours opened — the overnight queue. Each new chat cost reply + 3-clip video set (25s pauses) ≈ 2 min ⇒ ~30-60 min to drain, buyers mid-drain looked "missed". Fix in `handleThread` post-reply: count genuinely-waiting rows (`isUnreadAnchor && snippetSuggestsBuyerLast`); if **≥3**, DEFER this chat's videos (`"video deferred — N buyers waiting"`) and return — the existing quiet-chat revisit path (`maybeSendVideo(id, name, true)` on `!turn`) delivers them once the queue clears, still exactly once per chat. Replies-first cuts a 15-buyer drain to ~8-10 min. Also: 2nd Messenger tab in the SAME profile = safe 2× throughput (thread-lease was built for it) — recommended to operator for busy accounts, no code needed.
- **v0.21.1 — self-update folder-name bug + dangerous-download surfacing:** operator: "none updated to newest". (1) **Real bug**: `subsell-installer.zip` Extract-All lands in `Downloads\subsell-installer\subsell-extension\` — NOT in `SUD_BASES` → probe fails → auto-update silently OFF on every machine installed from the installer-named zip. `SUD_BASES` now covers both zip names, nesting, direct-extract, and " (1)" re-download variants. (2) `sudDownload` now detects Chrome's dangerous-file hold (`item.danger` set, e.g. .js from the web) → cancels + writes explanatory `sudStatus` instead of a silent 90s timeout. NOTE: machines on **pre-0.20** versions have NO updater and can never self-update — they need the one-time zip install first; check popup title version / presence of the "⬇ Update now" button.
- **v0.21.0 — Master Closer option (`closerIntensity`):** new synced setting `closerIntensity` (`soft` | `medium` | `master`, default `medium` ≈ prior behavior) in DEFAULTS, dashboard (General tab select under Closer mode), options.html/js, SETTINGS-REFERENCE. In `buildSystemPrompt` (only when `closerMode` on): `soft` = one gentle invite; `medium` = balanced guide + end-with-a-question; `master` = full sales-psychology playbook — micro-commitment ladder, assumptive/two-option close ("aujourd'hui ou demain?"), **reserve technique** ("je te le mets de côté — c'est à quel nom?"), value-stack before price, HONEST-urgency-only rule (never invent buyers/deadlines), objection counters (price/distance/think-about-it/shipping→[HUMAN]), exactly ONE advancing question per message, stop-selling-after-yes (+[VISIT:yes]), mirror buyer language/length/energy. Docs cache-buster bumped to 20260720.
- **v0.20.2 — "2 of 3 videos" + follow-ups rarely firing:** (1) **Partial set locked forever**: one failed clip download → files=2/3 → sent + locked. Now the set must be COMPLETE (`files.length === local+central expected`) before anything sends; otherwise `recordVideoFail` + retry later. (2) **Resume-tail**: operator navigation mid-set (their 25s between-gap = ~2-min window) stopped the set after clip 2 forever. The break now records `resumeFrom = first UNATTEMPTED index` in `videoSentThreads[id]` (+ `videoLocked.delete`), and a later visit sends ONLY that untouched tail (resume mode skips the has-video/DOM checks + first delay; marker cleared before sending — zero duplicate risk; attach-FAILED clips are never retried, only never-attempted ones). (3) **Follow-ups**: first-notice restarted the whole quiet clock (old quiet chats waited 2h AGAIN) → `sidebarQuietMs(anchor)` parses the row's "· 22m/2h/3j" tag; if already ≥ threshold, eligible immediately. A Claude `[SKIP]` used to fully reset the clock (few skips = never) → now pushes only HALF a period. `maybeFollowUp(id, name, anchor)` signature.
- **v0.20.1 — thread-lease fixes ("another tab is handling this chat"):** operator saw a hot buyer skipped with that status. Analysis: (1) reloads/updates mint a new `TAB_UID`, so a lease held by the dead pre-reload instance blocks its thread up to `LOCK_MS` (2 min) — expected, self-heals; (2) **amplifier bug**: `lastOpened[tid]` was stamped BEFORE `acquireThreadLock`, so a lock collision also burned the 4-min unread floor → hot buyer deferred ~5 min to the overdue lane. Fixed: stamp only AFTER the lease is won → blocked chats retry on the next 8s scan; status reworded ("waiting — … auto-retries"). (3) **latent double-reply risk**: legit handles (delay+typing+videos) outlive the 2-min lease and it was never renewed → a second window could steal it mid-send. Added `refreshThreadLock(id)` re-stamps at: post-open, after the response-delay sleep, and before `maybeSendVideo`.
- **v0.20.0 — BUILT-IN cloud self-update (installer deprecated — Defender flagged it):** the .bat/schtasks pipeline = download+hidden-persistence = the literal malware pattern ⇒ AV "virus detected" on operator machines; never coach AV bypass. Replaced with an in-extension updater using only Chrome APIs: hourly (self-throttled) `cloudSelfUpdate()` fetches `SUD_RAW manifest.json` (~1KB); if newer, downloads `SUD_FILES` (manifest.json **last** — the disk-watcher only reloads once it lands) via `chrome.downloads` (`conflictAction: overwrite`, `saveAs:false`, history `erase`d) into the extension's own folder, then `selfUpdateCheck()` reloads when no tab is mid-send. **Requirement:** unpacked folder must be inside **Downloads** (`subsell-extension` or the Extract-All-nested variant — `SUD_BASES`), verified by a data-URL **probe file** (`.sud-probe` written then fetched via `chrome.runtime.getURL`; `removeFile` after) — never guessed; wrong location ⇒ `sudStatus` explains in popup. New `downloads` permission; popup "⬇ Update now" button → `CHECK_UPDATE` handler; `sudStatus`/`sudBase`/`sudLastCheck` in storage.local. `deploy/install-subsell-auto-update.zip` **removed**; .bat/.ps1 kept as deprecated reference; AUTO-UPDATE-SETUP.md rewritten (per machine once: zip → Extract All in Downloads → Load unpacked → popup "Update now" shows "Up to date ✓").
- **v0.19.0 — never-miss ledger + two-lane queue (operator-specified policy):** operator wants "instant reply to the newest, but no convo ever skipped — constant verification". Implemented: (1) **`waitingSince` ledger** (persisted via `persistDedup`, hydrated at boot): every scan, every rendered row whose preview reads buyer-last is stamped first-seen; entries clear ONLY at terminal resolutions in `handleThread` (`clearWaiting(id, sidebarId)` on: replied ✓, [HUMAN], reply-cap stop, lastHandled-match, isOwnEcho, genuinely-idle read) — NOT on errors/aborts/suspicious reads, so failures keep the chat owed. (2) **Two-lane `pickTarget`**: LANE 0 = anyone on the ledger > `OVERDUE_MS` (5 min), oldest first, beats everything; LANE 1 = newest genuinely-waiting buyer (dot + buyer snippet, topmost row) → instant service for hot leads; stale-dot filter retained; P2/P3 unchanged. Worst-case wait is now bounded ≈ OVERDUE_MS + queue drain, FIFO within overdue. (3) Suspicious-read loop capped: 3 consecutive sidebar-says-buyer/chat-says-idle reads → accept idle + clearWaiting (stops permanently-buyer-looking group snippets from cycling). (4) Popup tick now shows `waitingCount`.
- **v0.18.5 — P1 queue fairness (FIFO) + stale-dot filter:** operator screenshot: Troy's "Is this still available?" unanswered 20 min while `unread count: 10` and the bot answered fresher chats. Two P1 flaws: (1) picker took the FIRST eligible unread = TOPMOST = newest-first ⇒ a stream of fresh buyers starves older ones; now serves the **oldest waiting** (last matching row, sidebar is recency-sorted = FIFO). (2) P1 trusted the dot alone and OVERRIDES cooldowns — on hidden/minimized tabs FB often never clears dots after we reply, so ghost-dot chats were re-opened every ~4 min, eating the queue; P1 now ALSO requires `snippetSuggestsBuyerLast` (a dot with a "You: …" preview is stale ⇒ falls to P3 idle rotation). Both changes confined to `pickTarget`.
- **v0.18.4 — fleet visibility + self-updating updater:** (1) `mirrorToCloud` appends the running version to the machine label (`"Shop PC · v0.18.4"`), so the dashboard's Activity tab per-machine breakdown doubles as a **fleet update monitor** (no schema change — rides the existing `machine` column). (2) Popup header shows `vX.Y.Z` (`#ver`). (3) `deploy/update-subsell.ps1` now **re-downloads itself** from GitHub at the end of each run (step 4, >500-byte sanity check), so updater improvements also roll out with zero touch. Answered operator: installer runs ONCE per computer, ever; every later update is fully automatic (hourly task + self-reload watcher).
- **v0.18.3 — group-name adoption + quarantine of unopenable threads:** operator screenshot (buyer waiting 7 min, `last error: couldn't open thread 2430567940773366`, current thread "Skylie **+1 other** · Iphone 13 pro"): the v0.18.1 adoption's header check required the FULL sidebar label ("Skylie +1 other") verbatim in the open chat — group headers render "Skylie and 1 other"/member lists, so adoption failed forever AND the bot kept burning cycles re-trying that thread while real buyers waited. Fixes: (1) header match now uses the **first participant only** (`split(/\+|&|,| et | and /i)[0]`), case-insensitive; (2) **quarantine**: `openFails[sidebarId]` counts consecutive failed opens — at 3, the thread is parked with a **30-min cooldown** ("thread won't open (3×) — parked") so it can't starve the queue; strikes cleared on any successful open.
- **v0.18.2 — zero-click fleet updates (self-update from disk):** Chrome forbids remotely-hosted code, so updates must land as FILES. New pipeline: `deploy/update-subsell.bat` (scheduled hourly via Task Scheduler, see `deploy/AUTO-UPDATE-SETUP.md`) downloads the latest zip from the GitHub raw link and `robocopy /MIR`s it into the fixed unpacked folder `C:\subsell-extension`; background's `selfUpdateCheck()` (10-min alarm) fetches its OWN `manifest.json` via `chrome.runtime.getURL` (unpacked extensions serve resources live from disk), and when the on-disk version ≠ loaded version calls `chrome.runtime.reload()` — but never while any Messenger tab reports `busy` (mid-send; rechecks next tick). `onInstalled` → `reinjectAllTabs()` completes the cycle. One-time per machine: fixed folder + Load unpacked from it + schtasks entry. Old `deploy/DEPLOY.md`/`subsell-policy.reg`/`update.xml` (enterprise-policy route) left as-is but superseded — that route needs managed Windows.
- **v0.18.1 — adopt FB's redirected thread id ("couldn't open thread" → opened-but-never-replied):** operator's popup showed `last error: couldn't open thread <id>` while that chat sat visibly open and unanswered. Cause: for some chats (group-style threads — "X changed the group photo"), clicking the sidebar row opens the chat under a DIFFERENT canonical `/t/<id>` than the row's href; `location.href.includes(id)` failed, the bot abandoned — but the chat HAD opened (dot killed) → forgotten forever, every visit. Fix in `handleThread`: on mismatch, extract the URL's `/t/<urlId>`; if it differs and the open chat's header contains the row's name (`label = name.split("·")[0]` found in `getMain().innerText`), ADOPT `urlId` as `id` (all state — lastHandled, replyCounts, videoSentThreads, stillOnThread — keys on the canonical id, consistent across visits since every visit re-adopts) and mirror the entry cooldown; abandon only when genuinely a different/failed page. Known minor: follow-up alarms look up anchors by canonical id and won't match the sidebar href for these chats → alarm-path follow-up skipped (smart follow-ups via normal visits still work).
- **v0.18.0 — keep-forced-open + deep-scan of the virtualized sidebar:** (1) **`ensureMarketplaceTab()`** (heartbeat, enabled-only): if NO messenger.com/facebook.com tab exists at all (employee closed it, Chrome restarted), the background opens `messenger.com/marketplace/` itself — `pinned:true` (tiny, hard to close accidentally), `active:false` (never steals focus), broad facebook.com guard so a login page counts as open (no tab spam), plus a persisted 10-min cooldown (`lastAutoOpenAt`). (2) **Deep-scan:** FB's sidebar is virtualized (~20 rendered rows; below-the-fold chats are invisible to the bot). When `document.visibilityState === "hidden"` (minimized ⇒ nobody watching) AND a scan found no eligible target, `deepScanStep()` scrolls the sidebar container (`sidebarScroller()` walks up from an anchor to the first scrollable ancestor) down ~0.8 viewport per idle cycle, wrapping to top at the bottom — deeper conversations get rendered and picked up by later scans. Never scrolls a visible tab (operator may be browsing).
- **v0.17.1 — "opened then forgotten" + short-greeting mute, three root causes:** (1) **`isOwnEcho` muted short buyer messages**: `recentSent` is global across chats and the exact match had no length floor, so a buyer opening with "allo,"/"oui"/"merci" was skipped as "our own message" if the bot ever sent that word anywhere (the Luc-"allo," case). Exact match now requires `length >= 10` (substring floor stays >12); bubble color remains the primary anti-self-reply. (2) **Suspicious-read recheck**: opening a chat KILLS its unread dot; when the sidebar said buyer-waiting (`isUnreadAnchor || snippetSuggestsBuyerLast`, snapshotted BEFORE `anchor.click()`) but the settled read says you-last (slow Remote-Desktop render), cooldown is now **2 min** (re-check) instead of the 10-min idle shelf — the core "opens, shows opened, forgotten" fix. (3) **Gate before opening**: outside business hours / hourly / daily caps, `scan()` now pauses BEFORE picking a target (via `GET_STATUS`) — the old flow opened chats overnight, marked them all read, then skipped replying, destroying every buyer-waiting signal by morning. Dots now survive until the bot may actually answer.
- **v0.17.0 — SELF-RESTART (background-driven) + sticky-dot starvation fix.** Built on the proven one-chat-per-scan base (no burst). (1) **Dead-tab watchdog:** heartbeat (1/min) now PINGs every Messenger tab; no answer (content script dead — crashed page/renderer/broken SPA) for **3 consecutive misses** (6 if `tab.active`, politeness) → `chrome.tabs.reload(tab)` — the operator's manual fix, automated. Misses persisted in `chrome.storage.local.tabHealth` (`{misses, reloadAt}` per tabId) so MV3 worker restarts don't reset the count; `tab.status === "loading"` never counts as a miss; entries for closed tabs pruned. (2) **Freshness reload:** stale-but-alive Messenger (list stops updating after many hours open) → every **6h per tab**, only when PING says `busy=false` AND tab not active, max one tab per cycle → `tabs.reload`. PING response now includes `busy`. (3) **Sticky-dot starvation (the "missing convos"):** operator's sidebar shows blue dots that never clear (reactions/glitches); with the 45s unread re-open floor the bot cycled those dot-chats forever and starved the rest. Floor raised to **4 min** (`UNREAD_REOPEN_MS`) — floored dot-chats fall through to P2/P3 so rotation always progresses; a never-opened unread chat still fires instantly.
- **v0.16.2 — REVERTED the v0.16.1 burst (it caused a dead bot while away).** Operator confirmed v0.16.0 reliable; v0.16.1's multi-chat-per-wake-when-hidden made the bot go fully dead after they left the computer — on a minimized, Chrome-throttled tab the long multi-chat busy period collided with the 6-min watchdog (watchdog reset `busy` mid-handle → overlapping `handleThread` → corrupted/stuck). Reverted to **one chat per scan** (the proven 0.16.0 model), keeping the clean `pickTarget()` selection. So v0.16.2 scan behavior == v0.16.0. **Do not reintroduce multi-chat-per-scan on hidden tabs.** Throughput-when-minimized is a Chrome-throttling limit; the zero-risk answer is keeping a window un-minimized (scans every 8s vs ~1/min).
- **v0.16.1 — throughput when all windows minimized + setting-wiring audit (REVERTED in 0.16.2):** operator runs ~12 Chromes all MINIMIZED → Chrome throttles each tab's scan timer to ~1/min, and the bot handled one chat per scan → a burst of buyers starves. `scan()` now, when `document.visibilityState === "hidden"`, processes up to **4 waiting chats per wake** (`pickTarget()` extracted; P1 unread → P2 snippet-buyer-last → P3 idle rotation, excluding already-handled), each through every per-chat guard; bounded by a **3-min elapsed cap** so a throttled cycle can never overlap the 6-min watchdog. Visible tab unchanged (burst=1, 8s interval). **Audit:** `humanCadence`/`skipChance`/`breakChance`, `wpmMin`/`wpmMax`, and warm-up **enforcement** are all currently **inert** (defined, never applied) — NOT the cause of missed chats, and wiring them would only reduce throughput; left as no-ops (fixed the misleading "content.js reads these" comment). Everything else (delays, caps, business hours, max-replies, closer/price/examples/guardrails, smart follow-up, videos) IS enforced. Honest limit: a minimized tab is still Chrome-throttled — keeping windows un-minimized = scan every 8s vs ~1/min.
- **v0.16.0 — "missing convos" + "no video at all" root causes:** (1) **Central video re-downloaded per chat, and 3 flaky downloads banned a chat from its video forever** → `fetchVideo` now caches by URL in `storage.local` (`videoCache`, one download per machine), and the `VIDEO_MAX_TRIES` give-up **expires after 24h** (pause, not ban). (2) **v0.15.1's FR detection could match buyer-typed text** ("je veux voir cette vidéo") → chat falsely marked served → never sent; regex tightened to FB's card wording only (`…vidéo à l'annonce`, `autoriser les autres acheteurs`, `mettre à jour l'annonce`, EN equivalents). (3) **Operator opening a chat clears the unread style → bot deprioritized it** (Pedro case): new PRIORITY 2 `snippetSuggestsBuyerLast(anchor)` — sidebar preview not starting with You:/Vous:/system lines ⇒ buyer likely waiting ⇒ visit before idle rotation (cooldowns respected; order-only, all guards unchanged). (4) Popup "Today" now shows the ENFORCED cap (`fullDailyCap`) — was showing the warm-up target ("34/10" confusion; warm-up was never enforced in GET_REPLY_SIMPLE).
- **v0.15.3 — self-heal from Facebook's OWN error page:** the operator found tabs stuck on FB's "This page isn't available right now / Reload Page" screen (debug showed `marketplace anchors: 0`, `last error: thread didn't load`, but the bot alive + Activity `✓ reporting`). `scan()` now, when `anchors.length === 0` AND `onFacebookErrorPage()` (matches FB's literal error wording, guarded by `body.innerText.length <= 3000` so it can NEVER match a real Messenger) AND a 2-scan streak AND a 4-min cooldown, navigates to `MP_INBOX` to recover. Deliberately narrow (explicit error text only — removed the riskier "blank page <200 chars" heuristic) so it cannot fire on a working page. This is a DIFFERENT cause than v0.15.2's Chrome-discard; together they cover both "reload page" sources.
- **v0.15.2 — stop Chrome discarding Messenger tabs ("reload page" next day):** Chrome's Memory Saver unloads idle background tabs after hours, so the operator returned to a "reload page" prompt (the bot kept running on the other tabs/heartbeat). `keepTabAlive(tabId)` = `chrome.tabs.update(tabId, {autoDiscardable:false})` is now applied to every Messenger tab on each heartbeat (1/min, when enabled) and on `reinjectAllTabs()`. Uses the existing `tabs` permission (no new permission → no re-approval). A genuine renderer crash still needs a reload — this only prevents the proactive memory-saver discard, which is the common case.
- **v0.15.1 — robust video dedup on OLD chats + one-click wake:** (1) `chatAlreadyHasOurVideo()` still missed chats that already had a video (old chats, or where the `<video>`/`mm:ss`-badge/geometry checks fail) → re-sends. Added a **side/geometry-independent primary signal**: the **"Add video to listing / Allow other buyers to see this video / Update listing"** card (EN+FR) that Facebook injects ONLY after the SELLER sends a video to a listing — its presence in `main.innerText` ⇒ we already sent one here ⇒ skip. This is the reliable catch the badge/`<video>` checks were missing. (2) Scanning: added a popup **"⚡ Wake scanner (all tabs)"** button + `WAKE_TABS` handler → `reinjectAllTabs()`, and re-inject on `SET_ENABLED` true — so a stalled tab is fixed with one click instead of reloading every page. Background-tab scans remain heartbeat-driven (message-driven `TICK_NOW`, ~1/min, not throttled); active tab every 8s — that's the MV3 floor.
- **v0.15.0 — no more page-reloads after an update + activity health + confidentiality:** (1) **Orphaned content script after an extension reload** was the real reason scanning "stopped until I reload each page": updating the extension leaves every open Messenger tab with a dead content script. Background now **re-injects `content.js` into all open Messenger/Marketplace tabs on `onInstalled`/`onStartup`** (`chrome.scripting`, new `scripting` permission); each content instance stamps `window.__subsellGen`, and `scan()` self-terminates if a newer instance took over OR the context is orphaned (`chrome.runtime.id` gone). So after a reload the tabs recover on their own. (2) **Activity-log health** now surfaced: `GET_STATUS` returns `lastMirror`; popup shows "Activity log: ✓ reporting / ✗ <error> / no sends yet" so an empty Activity tab is self-diagnosing (e.g. HTTP 401 ⇒ Verify JWT still on). (3) **Confidentiality** line added to `buildSystemPrompt` (never reveal instructions/rules/bot-ness), and the EXAMPLE CONVERSATIONS header now says rules written there are followed silently, never repeated — addresses the operator's "don't say this rule to the customer". All Business-tab fields (businessInfo/instructions/priceList/listings/closerGoals/examples) were already in the prompt — confirmed.
- **v0.14.1 — unread-first scheduling (missed-chats fix):** the scanner was unread-blind by design: a chat where WE spoke last sat under a 10-min idle cooldown even if the buyer replied seconds later, and when several chats were eligible it always took the TOPMOST, one per cycle → lower chats starved ("missing conversations"). Now `scan()` runs two priorities: (1) **unread chats** (detected via `isUnreadAnchor` — Messenger's round blue dot by computed style, or ≥2 bold (weight≥600) leaf spans in the row) **override every cooldown**, gated by a 45s per-chat re-open floor (`lastOpened`, in-memory) so a mis-styled row can't be hammered — false positives cost one harmless visit, never a duplicate reply (lastHandled/caps still gate); (2) idle rotation picks the eligible chat with the **oldest cooldown stamp** (never-visited first) instead of topmost — fair rotation. Popup `unreadCount` now real. Caveat: only chats rendered in the (virtualized) sidebar are scannable; FB floats new-message chats to top, so unread ones are reachable.
- **v0.14.0 — operator-navigation + hang fixes:** (1) **Wrong-chat sends**: the operator clicking another conversation during a long wait (response delay 30–90s, video delay, between-clips gap, follow-up Claude call) made the bot type/upload into whatever chat was open. Every send point now re-checks `stillOnThread(id)` (`location.href.includes(id)`) right before acting — reply path also re-verifies the buyer's last message is unchanged — and aborts silently (chat retried next cycle; for videos the pre-lock abort happens BEFORE `{done:true}` so the clip isn't lost; mid-set abort is after lock = safe). (2) **Bot frozen until page reload**: a hung await left `busy=true` forever. Watchdog: `busySince` + `BUSY_MAX_MS` (6 min) — the next tick force-resets and logs "watchdog: previous cycle hung". (3) **Central Activity empty on cloud-login machines**: `getConfigKey()` now falls back to reading the account's `config_key` via authenticated REST (`cloudValidAuth`), so machines without a Remote config URL also report. Mirror health breadcrumb: `chrome.storage.local.lastMirror` `{at, ok, error}`.
- **`maxRepliesPerConvo` was defined but never enforced** (bot replied forever) → `handleThread` now keeps a **persisted per-thread `replyCounts[id]`** (chrome.storage.local, capped/persisted via `persistDedup`, hydrated at boot). Gate runs before the Claude call: if `replyCounts[id] >= maxRepliesPerConvo` (cap>0) the chat goes silent for good (`convoCapBehavior` `stop`/`notify`); the counter increments **only on a confirmed `typeAndSend`**, so **videos and follow-ups never count** (those are separate paths/caps). A capped chat still gets its one-time demo video (idempotent `maybeSendVideo`). Set to N → at most N text replies for the whole conversation, even if the buyer keeps asking.

## Gotchas
- Messenger uses a **Lexical** contenteditable; `execCommand` insert/delete works but is finicky — hence the "insert once + invisible-tolerant verify + send once" pattern.
- Sender detection is now **color-first** (blue/gradient = us, gray = buyer) with geometry only as a last resort and **bias-to-silence on ambiguity**. If self-replies ever return, the color thresholds in `looksLikeOurBubble` likely need tuning to the current Messenger theme (log a known own-bubble and buyer-bubble `getComputedStyle().backgroundColor`/`backgroundImage`).
- The operator runs on a **Chrome Remote Desktop** (laggy) with **one Chrome profile per Facebook account**.
- `enabled` (on/off), the local mp4 upload, rate-limit counters, logs are **per-machine**; `cooldowns`/`lastHandled`/`recentSent`/`videoSentThreads`/`followUpState` are persisted per-machine (keyed by threadId).

## Deferred / known remaining (from the 3-agent audit, not yet implemented)
- **Avatar/attribution as the positive buyer signal (audit 1, finding 6):** color is the current positive buyer signal; the buyer's row-start avatar / "· Buyer" attribution would be even more robust but the exact selectors must be verified against the live messenger.com build (FB obfuscates classes).
- **`FETCH_VIDEO` base64 over messaging (audit 2, D2):** large mp4s are base64'd through `chrome.runtime` messaging; size-cap or switch to blob-URL for big files.

## Build / test / ship
- No bundler. Validate: `node --check background.js content.js options.js popup.js docs/app.js`.
- Rebuild the download: zip the extension files into `dist/subsell-extension.zip` (everything except `docs/`, `supabase/`, `.md`, `dist/`) **and copy it to `dist/subsell-installer.zip`** (same artifact, operator-facing name — keep BOTH refreshed every release).
- Operator installs via **Load unpacked** per Chrome profile; pastes the config URL or logs into Cloud sync.
- **Always test reply quality in the extension's Settings → Test responses (no Facebook) before going live**, then supervise the first few real chats.
- Commit + push to `claude/wizardly-noether-Oi6vP`.

## Likely next steps
- Keep hardening the live DOM reliability (Facebook layout drift).
- Optional: a "Test responses" tab + a cross-machine Activity log in the web app (not yet built).
